
type HeaderProps = {
  title: string;
};

const Header: React.FC<HeaderProps> = ({ title }) => {
  return (
    <header className="p-1 h-16 border-b-2 border-white flex justify-center items-center w-1/2 mx-auto overflow-hidden">
      
    </header>
  );
};

export default Header;
